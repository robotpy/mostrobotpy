extra_includes:
- wpi/sendable/SendableBuilder.h

classes:
  wpi::Sendable:
    methods:
      InitSendable:
        virtual_xform: |-
          [&](nb::callable fn) {
            auto builderHandle = nb::cast(builder, nb::rv_policy::reference);
            fn(builderHandle);
          }
