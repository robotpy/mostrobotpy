classes:
  frc::DigitalSource:
    methods:
      DigitalSource:
      GetPortHandleForRouting:
      GetAnalogTriggerTypeForRouting:
      IsAnalogTrigger:
      GetChannel:

inline_code: |
  cls_DigitalSource
    .def("__repr__", [](nb::handle self) {
      nb::object type_name = self.get_type().attr("__qualname__");
      int channel = self.cast<DigitalSource&>().GetChannel();
      return nb::str("<{} {}>").format(type_name, channel);
    });
