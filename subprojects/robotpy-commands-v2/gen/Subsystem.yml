---

extra_includes:
- frc2/command/Command.h

inline_code: |
  #include <src/Subsystem.cpp.inl>

classes:
  Subsystem:
    shared_ptr: true
    methods:
      Periodic:
      SimulationPeriodic:
      SetDefaultCommand:
        ignore: true # in Subsystem.cpp.inl
      GetDefaultCommand:
      GetCurrentCommand:
      Register:
