---

extra_includes:
- frc2/command/Command.h

classes:
  Subsystem:
    shared_ptr: true
    methods:
      Periodic:
      SimulationPeriodic:
      SetDefaultCommand:
        ignore: true # in Subsystem.cpp.inl
      GetDefaultCommand:
      GetCurrentCommand:
      Register:
